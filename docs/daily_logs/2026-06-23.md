## 2026-06-23

**Active branch:** feature/2-core-endpoint
**Issue:** #2

### What I built
- Built the Docker image from the Dockerfile and ran a container from both the terminal and Docker Desktop, confirming the /health endpoint returns {"status": "ok"} from inside the container.
- Wrote the POST endpoint for language prediction with its Pydantic request model, and confirmed it works end to end with a curl request (returning the sent value for now, the model is not connected yet).
- Experimented with the request validation model: started with a custom BeforeValidator for trimming plus Field(min_length=1), then refactored to the cleaner StringConstraints(strip_whitespace=True, min_length=1), which strips whitespace and rejects empty-after-trim input in one declarative step. Renamed the model to PredictionInput and added a description and a json_schema_extra example.
- Extended ADR-003 with the full status code taxonomy (each code marked automatic, manual, or stretch-dependent), the small error_code namespace, the decision to validate structure not meaning (empty input rejected, semantically thin input passed to the model), and the principle that low confidence is a successful response, not an error.

### What I learned
- Content-Type matters: curl -d defaults to form-encoding, so a JSON body needs -H "Content-Type: application/json" (or curl --json) or FastAPI applies the wrong parser. The body bytes are identical, the header is the label telling the server how to read them.
- Pydantic v2 idioms: Annotated[str, StringConstraints(...), Field(...)] is the modern way to stack metadata. StringConstraints handles standard string operations (strip, length) declaratively; a custom BeforeValidator is only needed for logic the built-ins cannot express.
- Error design: the real error space for this API is narrow. Validate that input structurally exists, but let the model judge whether content is meaningful (expressed through confidence). The consumer applies its own business rules to the score, the API stays single-purpose and reusable.

### Blockers / open questions
- Need to verify by testing that strip_whitespace runs before min_length (a whitespace-only body should return 422). Assumed, not yet confirmed.
- ADR-003 still documents some codes (429, 500, 503) that depend on features not yet implemented. Flagged in todo to re-check and remove any the API cannot actually produce.

### Next session
Reconstruct the full request/response mental model in detail (what FastAPI does, what Pydantic does), read the ResponseModel docs, then hypothesize how the ML model fits into the flow before wiring it in and testing.
